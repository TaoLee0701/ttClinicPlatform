spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ttClinicPlatform?serverTimezone=GMT%2B8
    username: root
    password: 1234

mybatis-plus:
  # 如果是放在resource目录 classpath:/mapper/*Mapper.xml
  mapper-locations: classpath:mapper/*Mapper.xml 
  #实体扫描，多个package用逗号或者分号分隔 
  type-aliases-package: clinicplatform.entity
  
logging.level.mycinema.mapper: debug   #显示项目中的Mapper调试日志